spring.application.name=ScoutSummoners

# SQLite Database Configuration
spring.datasource.url=jdbc:sqlite:scoutsummoners.db
spring.datasource.driver-class-name=org.sqlite.JDBC

# JPA Configuration
spring.jpa.database-platform=org.hibernate.community.dialect.SQLiteDialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

# Thymeleaf Configuration
spring.thymeleaf.cache=false

# File Upload Configuration
spring.servlet.multipart.enabled=true
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB
file.upload-dir=uploads

# Security
spring.security.user.name=${SPRING_SECURITY_USER:XXXXXXXXX}
spring.security.user.password=${SPRING_SECURITY_PASSWORD:XXXXXXXXXXX}

# Google reCAPTCHA Configuration
# Get your keys from: https://www.google.com/recaptcha/admin
recaptcha.site-key=${RECAPTCHA_SITE_KEY:XXXXXXXXXXXXXXXXXXXXXXXXXX}
recaptcha.secret-key=${RECAPTCHA_SECRET_KEY:XXXXXXXXXXXXXXXXXXXXXXX}
recaptcha.verify-url=https://www.google.com/recaptcha/api/siteverify

# Mailer To Go Email Configuration (for Heroku deployment)
# For Heroku: These are set automatically when you add the Mailer To Go addon
# For local development: Get values from: heroku config
spring.mail.host=${MAILERTOGO_SMTP_HOST:XXXXXXXXXXXXXXXX}
spring.mail.port=${MAILERTOGO_SMTP_PORT:XXXX}
spring.mail.username=${MAILERTOGO_SMTP_USER:XXXXXXX}
spring.mail.password=${MAILERTOGO_SMTP_PASSWORD:XXXXXXXX}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.from=${MAILER_FROM_EMAIL:XXXXXXX}
admin.email=${ADMIN_EMAIL:XXXXXX}

# Application Base URL (for email links and redirects)
app.base-url=${APP_BASE_URL:XXXXXXXXXXX}
